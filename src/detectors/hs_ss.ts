// import { BotKind, ComponentDict, DetectorResponse, State } from '../types'

// export function detectSessionSorage({ isSessionStorage }: ComponentDict): DetectorResponse {
//   if (isSessionStorage.state !== State.Success) return false
//   if (typeof isSessionStorage.value !== 'object') return BotKind.Unknown
// }
